# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:
    google_user_provider:
        class: AppBundle\Security\GoogleUserProvider
        arguments: ["@fos_user.user_manager.default", { google: googleId }]

    google_contact_provider:
        class: AppBundle\Service\GoogleContactProvider
        arguments: ["@guzzle.client"]